import { init, keyframes } from './scrollAnimation'
import { animate } from './customAnimation'

window.cudeAnimations = {
  init,
  keyframes, 
  animate
}

export {
  init,
  keyframes,
  animate
}
